<%= form_for person, :html => {:class => 'form-horizontal'} do |p| %>
  <div class='control-group'>
    <%= p.label :type, :class => 'control-label' %>
    <div class='controls'>
      <%= p.select :type, { 'Individual' => 'Individual', 'Board Member' => 'BoardMember', 'Advisor' => 'Advisor' } %>
    </div>
  </div>
  <div class='control-group'>
    <%= p.label :first_name, :class => 'control-label', :id => "first_name" %>
    <div class='controls'>
      <%= p.text_field :first_name, :name => 'first_name' %>
    </div>
  </div>
  <div class='control-group'>
    <%= p.label :last_name, :class => 'control-label', :id => "last_name" %>
    <div class='controls'>
      <%= p.text_field :last_name, :name => 'last_name' %>
    </div>
  </div>
  <div class='control-group'>
    <%= p.label :occupation, :class => 'control-label', :id => "occupation" %>
    <div class='controls'>
      <%= p.text_field :occupation, :name => 'occupation' %>
    </div>
  </div>
  <% person.phone_numbers.each do |phone_number| %>
    <%= p.fields_for :phone_numbers, phone_number do |pn| %>
    <div class='control-group'>
      <%= pn.text_field :label, :class => 'control-label' %>
      <div class='controls'>
        <%= pn.text_field :info %>
      </div>
      <%= pn.check_box :_destroy %>Destroy
    </div>
    <% end %>
  <% end %>
  <div class='control-group'>
    <div class='controls'>
      <%= p.submit "#{action}" , :class => 'btn', :name => "#{action}"  %>
    </div>
  </div>
<% end %>
