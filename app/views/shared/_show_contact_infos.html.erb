<dt><%= info_type.split("_").each{|word| word.capitalize!}.join(" ") %></dt>
<% contact.send(info_type).each do |contact_info| %>
  <dd><%= "#{contact_info.label}: #{contact_info.info}" %></dd>
<% end %>
