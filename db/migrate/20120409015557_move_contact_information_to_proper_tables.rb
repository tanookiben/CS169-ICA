class MoveContactInformationToProperTables < ActiveRecord::Migration
  def up
#    ContactInformation.all.each do |ci|
#      if ci.type == "PhoneNumber"
#        PhoneNumber.new(:callable_type => ci.contactable_type,
#                        :callable_id => ci.id,
#                        :label
  end

  def down
  end
end
